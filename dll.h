
# pragma once
////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////
# include <vector>
////////////////////////////////////////////////////////////////
using std::vector;
////////////////////////////////////////////////////////////////
namespace config {
////////////////////////////////////////////////////////////////
using off_t = int;

struct Link {
    off_t next = 0;
    off_t prev = 0;
};

class dll {
private:
    vector <Link> bufr;
    Link& ahead;
public:
    dll( int cap = 34 ):
        bufr( vector <Link> ( cap )),
        ahead( bufr.front() )
    {}
    off_t front() const {
        return ahead.next;
    }
    void insert( off_t off ){
        bufr[ off ].next = ahead.next;
        bufr[ off ].prev = 0;
        bufr[ ahead.next ].prev = off;
        ahead.next = off;
    }
    void unlink( off_t off ){
        bufr[ bufr[ off ].prev ].next = bufr[ off ].next;
        bufr[ bufr[ off ].next ].prev = bufr[ off ].prev;
    }
    off_t next( off_t off ) const {
        return bufr[ off ].next;
    }
    bool empty() const {
        return !ahead.next;
    }
    void dance( off_t off ){
        bufr[ bufr[ off ].prev ].next = off;
        bufr[ bufr[ off ].next ].prev = off;
    }
};

}

